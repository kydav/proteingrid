// Widget tests are left as stubs; the app requires Hive, Firebase, and native
// plugins to be initialised before rendering. The unit tests in test/data/ and
// test/core/ provide comprehensive logic coverage.
import 'package:flutter_test/flutter_test.dart';

void main() {
  // No widget tests in this file — placeholder removed.
}
